{
  "name": "<%= this.appname%>",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "initProject" : "npm install && cd approuter && npm install && cd .. && cd srv && npm install",
    "deploy": "run-s build:ui && cf push",
    "build:ui": "run-s  build:uimodule",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:uimodule": "ui5 build --config=web/uimodule/ui5.yaml --clean-dest --dest web/uimodule/dist --include-task=generateManifestBundle"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ui5/cli": "^2.14.10",
    "npm-run-all": "^4.1.5",
    "ui5-middleware-livereload": "^0.6.0"
  }
}
